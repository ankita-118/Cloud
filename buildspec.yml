version: 0.2

phases:
  install:
    commands:
      - pip3 install bandit

  pre_build:
    commands:
      - bandit -r . -f txt || true

  build:
    commands:
      - zip -r function.zip .

artifacts:
  files:
    - function.zip
